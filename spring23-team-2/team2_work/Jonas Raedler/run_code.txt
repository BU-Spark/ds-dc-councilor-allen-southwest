Simply choose the notebook of choice and run all the cells. Everything is ready to go and should produce the plots of that variable.

The only thing that you might need to change is the path for the .csv files and the path to the directory in which you want the plots to be saved (the data is always in the /data folder).